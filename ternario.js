//Operadores ternários

/* Exemplo de função arrow com operadores ternários */
const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'

//se for verdadeiro retorna aprovado, senão reprovado. É tipo um IF bem simplificado.

console.log(resultado(7.0))

